const data = {
    dia: 1,
    mes: 10,
    ano: 2021,
}

data.dia = 27; //altera o atributo mes dentro do objeto data
data.mes = 12; //altera o atributo mes dentro do objeto data

console.log(`${data.dia}/${data.mes}/${data.ano} `);
