const usuario = {
    email: "aluno@cod3r.com.br",

};

usuario.nome = "Aluno"; // criará o atributo nome dentro do objeto usuário recebendo "Aluno";
usuario.senha = 12345 ; // criará o atributo senha dentro do objeto usuário recebendo 12345;

console.log(usuario.email);
console.log(usuario.nome);
console.log(usuario.senha);
console.log(usuario);

/* console.log(typeof usuario.nome);
console.log(typeof usuario.senha); */ 